# Agent Notes

## Runtime

- Node.js: **24** (see `.node-version`)
- Package manager: **pnpm 11** (see `packageManager` in `package.json`)

## Shell / Commands

- Do **not** run commands in a sandbox.
- Before every shell command, **ask the user for confirmation** and wait for approval. Do not batch-execute without confirming each command.
